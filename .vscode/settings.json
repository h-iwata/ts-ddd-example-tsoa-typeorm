{
  // このプロジェクトではスペルチェック（Code Spell Checker / cspell）を使用しない
  "cSpell.enabled": false,

  // Lint・フォーマットはBiomeに統一（ESLint / Prettierは使用しない）
  "editor.defaultFormatter": "biomejs.biome",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.biome": "explicit",
    "source.organizeImports.biome": "explicit"
  },
  // エディタもプロジェクトのTypeScript（node_modules配下）を使う。
  // VSCode同梱版とバージョンがズレると、CIでは通るのにエディタだけエラーが出る
  "typescript.tsdk": "node_modules/typescript/lib",
  "typescript.enablePromptUseWorkspaceTsdk": true,

  // テストはソースの隣に置く方針。ツリーが倍に見えるのを防ぐため、エディタ上では対象ファイルに畳む
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.expand": false,
  "explorer.fileNesting.patterns": {
    "*.ts": "${capture}.test.ts, ${capture}.integration.test.ts"
  },

  "[typescript]": { "editor.defaultFormatter": "biomejs.biome" },
  "[json]": { "editor.defaultFormatter": "biomejs.biome" },
  "[javascript]": { "editor.defaultFormatter": "biomejs.biome" }
}
