# MySQL接続設定
# Docker外（ホスト）から実行する場合の設定。
# コンテナ内では docker-compose.yml の environment が使われる。
DB_HOST=localhost
DB_PORT=3307
DB_USERNAME=root
DB_PASSWORD=password
DB_DATABASE=ddd_example

# サーバー設定
PORT=3000
NODE_ENV=development
